# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Secrets and local env (do not use broad .env.* — it can ignore .env.example)
.env
backend/.env

# IDE / OS
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# Cursor workspace artifacts (not application source)
agent-transcripts/
agent-tools/
mcps/
terminals/
gate1_probe_result.txt

# Gate 2 — local Ollama (large; lives under ZY_BASE_DIR, copied with move_to_d_drive)
vendor/ollama/
models/ollama/

# Option C runtime dirs (logs/exports under ZY_BASE_DIR)
logs/
exports/
models/
!models/.gitkeep

# Flutter: duplicate scaffold at repo root (canonical app is under mobile/)
zy_smart_client/
