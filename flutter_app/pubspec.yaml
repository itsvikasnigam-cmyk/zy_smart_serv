name: zy_smart_flutter
description: ZY Smart Serv — Flutter shell for client_api (login, owner inbox, super_admin placeholder).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  web_socket_channel: ^3.0.1
  provider: ^6.1.2
  intl: ^0.19.0
  flutter_markdown: ^0.7.4+1
  markdown: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
